function marker(text, color, num) {
    document.write(`<p style="font-family: 'Comic Sans MS';  color: ${color}; 
     font-size: ${num}px;">${text}</p>`);
  }

  marker('Наш новый текст', 'Red', 50);